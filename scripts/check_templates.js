const fs=require('fs'); const files=['frontend-vue/src/views/admin/TeacherManagement.vue','frontend-vue/src/views/admin/StudentManagement.vue','frontend-vue/src/views/admin/MasterBroadsheet.vue']; for(const f of files){ const s=fs.readFileSync(f,'utf8'); const m=s.match(/<template>[\s\S]*<\/template>/i); console.log('\n==='+f+'==='); if(!m){ console.log('No template found'); continue;} const tpl=m[0]; const openCount=(tpl.match(/</g)||[]).length; const closeCount=(tpl.match(/>/g)||[]).length; const closingTags=(tpl.match(/<\\/g)||[]).length; console.log('chars:', tpl.length, ' < count:',openCount,' > count:',closeCount,' </ count:',closingTags); if(openCount!==closeCount) { console.log('Mismatch < vs >'); let lines=tpl.split(/\r?\n/); for(let i=0;i<lines.length;i++){ if(lines[i].includes('<') && !lines[i].includes('>')) console.log('line',i+1, lines[i]); } } }
